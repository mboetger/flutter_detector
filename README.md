# Flutter Detector

This project contains a simple shell script `detector.sh` that helps in detecting Flutter installations on Android devices.

## Usage

To execute the script, run the following command:

```bash
sh <(curl -0 https://raw.githubusercontent.com/mboetger/flutter_detector/refs/heads/main/detector.sh)
```
